# LinkedIn Draft - 2026-09-23

Worker Previews isolate branch state, but service bindings can still reach production.

Went through 40 links today. Here's what made the brief:

1. Introducing Worker Previews: Isolated preview environments for every change your agent makes
   Map those dependencies and test with explicit nonproduction bindings before treating a preview as an isolated full-system test.

2. langchain-anthropic==1.7.3
   If using this adapter, test schema validation and message ordering before upgrading; this is a targeted compatibility release, not a model-quality result.

3. Claude Opus 5.5 now available on AI Gateway
   Audit those request fields, use structured outputs for JSON, and test tool-selection misses before migrating.

4. v16.3.6
   Check affected image routes and prioritize the patched release; if an upgrade must wait, prevent those untrusted values from reaching SVG rendering.

5. When a file is uploaded, run the job on Neon
   Test retries and enable the trigger deliberately in a test branch, and keep unreviewed uploads private because processing happens after upload.

Full brief with all 40 researched links → https://thenightlylibrarian.com/briefs/2026-09-23

Draft status: not approved for posting.
